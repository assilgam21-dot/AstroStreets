#pragma once

// Size of one world tile in pixels. Sprites are authored at this resolution and
// drawn one-per-tile in world space (before the camera transform).
inline constexpr int TILE_SIZE = 64;
